# Need to change code so that annotations are before the code rather than after

print("hello again!")

# This line prints a simple greeting to the user so the script starts with a visible message.
# The print() function displays text inside quotation marks on the screen.

width = 15

# This line creates a variable named width and stores the number 15 in it.
# The equal sign assigns a value to the variable, and the variable can be used later in calculations.

height = 4

# This line creates a variable named height and stores the number 4 in it.
# The height value is needed to calculate the area of the rectangle.

area = width * height

# This line multiplies the width by the height to find the total area of the rectangle.
# In math, the area of a rectangle is found using the formula area = width × height.

print(f"The area of a rectangle with width {width} and height {height} is: {area}")

# This line displays a full sentence explaining the result of the calculation.
# The f-string allows us to insert the values of width, height, and area directly into the message.

print(area)

# This line prints only the numeric area value so the result is easy to read in a simple form.
# It shows the calculated answer without the extra words from the previous print statement.